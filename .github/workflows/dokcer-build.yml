name: Docker Multi-Arch Build

permissions:
    contents: read
    packages: write

on:
    push:
        branches: [ "main" ]

    pull_request:
        types: [opened, synchronize, reopened]

jobs:
    build-and-push:
        runs-on: ubuntu-latest

        steps:
        - name: Checkout repository
          uses: actions/checkout@v4

        - name: Set up QEMU
          uses: docker/setup-qemu-action@latest
        
        - name: Set up Docker Buildx
          uses: docker/setup-buildx-action@latest

        - name: Log in to GHCR
          uses: docker/login-action@latest
          with:
              registry: ghcr.io
              username: ${{ github.repository_owner }}
              password: ${{ secrets.GITHUB_TOKEN }}

        - name: Build and push (for PR)
          if: github.event_name == 'pull_request'
          uses: docker/build-push-action@latest
          with:
            context: .
            platforms: linux/amd64,linux/arm64
            push: true
            tags: ghcr.io/${{ github.repository_owner }}/${{ github.repository }}:pr-${{ github.event.number }}
            cache-from: type=gha
            cache-to: type=gha,mode=max

        - name: Build and push (for Main)
          if: github.event_name == 'push' && github.ref == 'refs/heads/main'
          uses: docker/build-push-action@latest
          with:
            context: .
            platforms: linux/amd64,linux/arm64
            push: true
            tags: ghcr.io/${{ github.repository_owner }}/${{ github.repository }}:latest
            cache-from: type=gha
            cache-to: type=gha,mode=max